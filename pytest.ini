[tool:pytest]
asyncio_mode = auto
addopts = --tb=short -v --import-mode=importlib
filterwarnings =
    ignore::DeprecationWarning:supabase.*
    ignore::PytestReturnNotNoneWarning
    ignore::RuntimeWarning
markers =
    asyncio: mark test as asyncio coroutine